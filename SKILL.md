---
name: information-literacy
description: 为信息素养选择题、判断题和具体资料查询拆解条件，选择原始来源并核验检索结果、文件版本、产品菜单和引用证据。适用于备赛、练习与明确允许的辅助答题；不提供题库或默认考试操作授权。
---

# 信息检索与证据核验

用宿主已有的搜索、浏览器和文件读取能力解决当前题目。此包不依赖私有题库、固定 MCP、特定账号或本机路径，不安装工具、不调整全局配置。先确认真正执行操作的上下文具有所需能力；主控能浏览不等于其他上下文也能浏览。

## 先确定当前任务

- 学习、复盘：取得原始材料与方法，明确硬阻塞后停止无效重试；没有默认单题时限。
- 限时练习：按当场时间、计分、返回规则安排投入，不预设固定秒数。
- 正式考核：只有当前规则明确允许且用户授权时才进入对应读题、检索、选择或提交操作；授权范围逐项适用，不从练习授权推导。保留用户指定的最终交卷确认。

开始前确认是否可返回、翻页是否保存/提交、截止时间及外部工具范围。未知时保守按不可返回处理：关键查证与选择核验必须在翻页前完成，不把“最后再查”当补救。不能用点击正式考试开始或交卷来测试功能，也不尝试绕过平台限制。

## 每题工作路径

1. **采集完整输入。** 保留题型、完整题干、全部选项和必要图片。提取否定词、对象标识、指定库/子库、字段、逻辑、精确/模糊、时间、版本、账户层级和计数口径。不按正文中的“提示”等词截断，不因 DOM 没有文字就忽略图片。
2. **区分问题类型。** 题面缺失先重新采集；旧题匹配失败不能套用旧答案；完整新题直接独立检索或推理，不向用户索要答案。纯逻辑与给定数据可本地求解。题号只是日志标识；复用必须匹配完整题设及选项文字，年份或否定词变化须重新求解。
3. **选择最短可靠路径。** 稳定概念查原始资料，指定数据库统计走该库，账户功能走实际入口，文件题读原件。搜索摘要和辅助库可以定位，但不能替代指定口径。按需读 [检索路由](references/search-routing.md)，不要默认加载全部参考资料。
4. **取得本次有效结果。** 表单写入不等于接受，点击提交不等于查询完成；核生效条件、新结果及至少一个符合条件的样本。切库、切模式或恢复认证后重新核对。旧结果、加载中的空表和访问失败均不能解释为零。按需读 [证据核验](references/evidence-checks.md)。
5. **形成候选与证据。** 单选比较选项含义；多选逐项标支持/排除/未知，再决定完整集合。核题设数量约束和否定词，不机械全选未知项，也不机械只选有直接证据的项。证据冲突优先核条件、版本和来源适用范围，不以模型多数票定案。
6. **有界决策。** 学习可保留多个条件候选。限时作答只有在规则允许、用户授权且已确认错答没有额外惩罚时，才可在证据耗尽后做明确标记的猜选；规则未知不默认错答无损。预留选择核验与最终确认时间；临近截止停止开新入口，报告剩余时间和缺口。不会返回的场次不能擅自跳题。
7. **按文字选择并验证。** 仅在已获操作授权时执行：先保存本题候选文字，再新读完整题型、题干、选项，确认页面未变、无上一题残留；按当前文字映射字母，点击后读取实际 selected/checked 文字集合。集合不一致先修复，不翻页；真正无法选择记操作失败。正常翻页后等新题面出现，不重复点击。查询失败与选择失败分开记录。

遇真实登录、验证码或机构认证，执行 [人工交接](references/browser-handoff.md)。不要求用户处理隐藏预置组件，不迁移认证凭据。

## 三种结论分别保存

- **事实证据**：`verified` 表示来源直接支持且满足当前题设；`partial` 表示间接来源或缺条件；`unavailable` 表示未取得。记录具体来源、时点、支持范围和冲突，不把“来源官方”单独当作充分证据。
- **作答建议**：当前选项文字、当前字母、置信度与 `direct / inferred / guess`。猜选可以后来被判对，但不能回写为当时已核验。
- **评分状态**：官方逐题明示、完整场次汇总所能支持的条件结论、或未知。没有逐题结果不能编造错误题号；条件推导必须写出题集、实际提交、计分方式与答案键稳定等前提。评分支持不升级产品事实证据。

动态数量、排名和菜单复用查询方法，不缓存为永久答案。历史首答、时间、未核验状态及成绩保持原样，后续发现只追加修正。人工协助、访问阻塞、实际操作是否完成与答案是否正确分开记录；单卷满分不证明陌生题能力。

## 默认答复

除非用户要求过程或复盘，保持四行：

```text
答案：当前字母＋选项文字（必要时标候选或无法判断）
关键依据：直接证据或推断，保留关键限定与缺口
置信度：高／中／低
来源：实际查过的原始页面或获准文件；无直接来源则明说
```

可只在临时上下文保留简短记录；用户要求落盘时使用其指定位置，避开凭据、个人资料和含认证参数的地址。不创建新的管理体系。受阻时说明最小缺失输入和可继续的工作。

本 Skill 是工作约定，不是浏览器锁、检索引擎或答案正确性证明。通用点击仍可能绕过检查；不得宣称技术上强制拦截。边界案例见 [合成示例](examples/cases.md)。
